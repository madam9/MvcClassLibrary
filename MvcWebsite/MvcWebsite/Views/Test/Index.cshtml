@model IEnumerable<MvcWebsiteSystem.Entities.Region>
@{
    ViewBag.Title = "Index";
    //Test Folder
}

<h2>Index</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RegionDescription)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RegionDescription)
            </td>
        </tr>
    }
</table>